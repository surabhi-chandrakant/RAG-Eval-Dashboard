version: "3.9"

services:
  kb-app:
    build: .
    container_name: company-kb-qa
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - kb_chroma:/app/chroma_db
      - kb_data:/app/data
    restart: unless-stopped

volumes:
  kb_chroma:
  kb_data:
